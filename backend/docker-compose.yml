services:
  mongodb:
    image: mongo:7
    container_name: market_village_mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
