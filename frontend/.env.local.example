# Standalone mode — market simulation API (mock/backend fallback).
NEXT_PUBLIC_SIM_API=http://127.0.0.1:8100

# Canonical "라이브(정석)" mode — live reverie servers.
# Data API (api_server): home / environment process+update + static game assets.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# Control API (control_server from reverie/backend_server): start / run / market event+state.
NEXT_PUBLIC_CONTROL_BASE=http://127.0.0.1:8001
