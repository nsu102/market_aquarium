# LIVE "라이브(정석)" mode — single merged reverie backend (live_server).
# Serves both /api/* (home / environment process+update + static game assets)
# and /control/* (start / run / market event+state) on the same port.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
NEXT_PUBLIC_CONTROL_BASE=http://127.0.0.1:8000
